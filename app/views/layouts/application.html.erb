<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CourseCompass</title>
  <!-- Bootstrap CSS (Latest version) -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Custom stylesheet for additional styling -->
  <link rel="stylesheet" href="<%= asset_path 'custom.css' %>">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  <%= csrf_meta_tags %>
  <%= favicon_link_tag 'https://admissions.ucr.edu/sites/default/files/styles/form_preview/public/2020-07/ucr-education-logo-columbia-university.png?itok=-0FD6Ma2' %>

</head>
<body>

<div class="navbar-brand-container">
  <%= link_to "CourseCompass", root_path, class: "navbar-brand" %>
</div>

<nav role="navigation" class="navbar navbar-expand-lg navbar-custom">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <%= link_to "Profile", profiles_path(id: session[:uid]), class: "nav-link #{'active' if controller_name == 'profiles'}" %>
        </li>
        <li class="nav-item">
          <%= link_to "Courses", courses_path, class: "nav-link #{'active' if controller_name == 'courses'}" %>
        </li>
      </ul>
      <ul class="navbar-nav">
        <li class="nav-item">
          <% if session[:uid] %>
            <%= link_to "Log Out", logout_path, method: :delete, class: "nav-link" %>
          <% else %>
            <%= link_to "Log In", login_path, class: "nav-link" %>
          <% end %>
        </li>
      </ul>
    </div>
  </nav>




  <main role="main" class="py-4">
    <div class="container">
      <%= render 'layouts/flash' %>
      <%= yield %>
    </div>
  </main>

  <footer class="footer mt-auto py-3 bg-light">
    <div class="container text-center">
      <span class="text-muted">© 2023 CourseCompass. All Rights Reserved.</span>
    </div>
  </footer>

  <!-- Bootstrap Bundle with Popper -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
